# Sanitized Cloud Run Job example

This directory demonstrates a bounded one-shot Cloud Run Job shape. It does not create service accounts, IAM bindings, registries, buckets, APIs, state backends or credentials, and it is not authorized for `terraform apply`.
